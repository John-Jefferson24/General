apiVersion: v1
kind: Service
metadata:
  labels:
    service: redis
    project: {{ .Values.projectName }}
  name: redis
  namespace: {{ .Values.projectName }}
spec:
  type: ClusterIP
  ports:
  - name: "6379"
    port: 6379
    targetPort: 6379
  selector:
    service: redis
