apiVersion: v1
kind: Service
metadata:
  labels:
    service: tdm-api
    project: {{ .Values.projectName }}
  name: tdm-api
  namespace: {{ .Values.projectName }}
spec:
  type: {{ .Values.traffic.ingresses.serviceType }}
  ports:
  - name: "8686"
    port: 8686
    targetPort: 8686
  selector:
    service: tdm-api
