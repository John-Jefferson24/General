apiVersion: v1
kind: ServiceAccount
metadata:
{{- if and .Values.authentication.role.key .Values.authentication.role.value }}
  annotations:
    {{ .Values.authentication.role.key }}: {{ .Values.authentication.role.value }}
{{- end}}
  name: snorkelflow-sa
  namespace: {{ .Values.projectName }}